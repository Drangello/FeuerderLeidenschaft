/**
 * Represents the keyboard input state.
 * Tracks which movement and action keys are currently pressed.
 */
class Keyboard {
    LEFT = false;
    RIGHT = false;
    JUMP = false;
    THROW = false;
}